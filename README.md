# Task 8 - Java Hello World with Maven

## Objective
Build and run a simple Java application using Maven.

## Steps Performed
1. Created a `HelloWorld.java` file with a print statement.
2. Added `pom.xml` for Maven project configuration.
3. Built the project using:
   ```bash
   mvn package
Ran the .jar file:

bash
Copy code
java -jar target/hello-java-maven-1.0.0.jar
Output
Copy code
Hello, Jenkins + Maven!
Files
src/main/java/HelloWorld.java

pom.xml

target/hello-java-maven-1.0.0.jar
